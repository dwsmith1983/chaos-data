name: validation-equals-fail-wrong-value
description: Phantom sensor writes status PARTIAL when rule expects equals COMPLETE — should exhaust validation
category: state-consistency
severity: moderate
version: 1
capability: validation/equals
target:
  layer: state
  filter: { prefix: "", match: "" }
mutation:
  type: interlock-phantom-sensor
  params:
    pipeline: "bronze-cdr"
    sensor_key: "hourly-status"
    status: "PARTIAL"
setup:
  pipeline: bronze-cdr
  pipeline_config:
    pipeline: { id: bronze-cdr }
    validation:
      trigger: "ALL"
      rules:
        - key: "SENSOR#hourly-status"
          check: equals
          field: status
          value: "COMPLETE"
    job: { type: command, config: { command: "echo test" } }
    schedule: { evaluation: { window: "1h", interval: "5m" } }
  sensors:
    hourly-status:
      status: COMPLETE
      sensor_count: 1000
probability: 1.0
safety:
  max_affected_pct: 100
  cooldown: 0s
  sla_aware: false
expected_response:
  within: 30s
  asserts:
    - type: interlock_event
      target: "VALIDATION_EXHAUSTED"
      condition: exists
