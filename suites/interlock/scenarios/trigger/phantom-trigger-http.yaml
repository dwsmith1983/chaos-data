name: phantom-trigger-http
description: Fire HTTP trigger without dependencies met; validation should block it
category: orchestrator
severity: severe
version: 1
capability: trigger_types/http
target:
  layer: orchestrator
  filter: { prefix: "", match: "" }
mutation:
  type: interlock-phantom-trigger
  params:
    pipeline: "bronze-cdr"
    job_type: "http"
setup:
  pipeline: bronze-cdr
  pipeline_config:
    job:
      type: http
      endpoint: "https://api.example.com/trigger"
  sensors:
    hourly-status:
      status: PENDING
      sensor_count: 0
probability: 1.0
safety:
  max_affected_pct: 100
  cooldown: 0s
  sla_aware: false
expected_response:
  within: 30s
  asserts:
    - type: interlock_event
      target: "VALIDATION_EXHAUSTED"
      condition: exists
